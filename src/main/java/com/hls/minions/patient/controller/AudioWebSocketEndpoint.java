
package com.hls.minions.patient.controller;
/*

import java.nio.ByteBuffer;
import javax.websocket.OnClose;
import javax.websocket.OnError;
import javax.websocket.OnMessage;
import javax.websocket.OnOpen;
import javax.websocket.Session;
import javax.websocket.server.ServerEndpoint;

//@ServerEndpoint("/audio")
public class AudioWebSocketEndpoint {
*/
/*
  @OnOpen
  public void onOpen(Session session) {
    System.out.println("WebSocket opened: " + session.getId());
  }

  @OnMessage
  public void onMessage(ByteBuffer message, Session session) {
    try {
      // For demo purposes, echo the received audio back to the client.
      // In a real application, process the audio and send back the response.
      session.getBasicRemote().sendBinary(message);
    } catch (Exception e) {
      e.printStackTrace();
    }
  }

  @OnClose
  public void onClose(Session session) {
    System.out.println("WebSocket closed: " + session.getId());
  }

  @OnError
  public void onError(Session session, Throwable throwable) {
    System.err.println("Error on session " + session.getId() + ": " + throwable.getMessage());
  }*//*

}

*/
